apiVersion: v1
kind: Service
metadata:
  name: ticket-generator
  namespace: ticket-booking-dev
spec:
  type: ClusterIP
  selector:
    app: ticket-generator
  ports:
    - name: http
      port: 3000
      targetPort: 3000
